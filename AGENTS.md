# Meridian

Read [CLAUDE.md](CLAUDE.md). It is the description of this project, and it is
the one that is kept current.

This file used to carry its own copy. The copy drifted — it was still calling
the UI layer "shadcn/ui planned" well after the move to HeroUI v3, and it had
never picked up the sections on the message tree, the transcript scroller,
logging or Android. A second description earns nothing and goes stale in
silence, so this one points at the first instead.

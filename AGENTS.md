# Meridian

Read [CLAUDE.md](CLAUDE.md). It is the description of this project, and it is
the one that is kept current — including the UI conventions (boardui tokens,
composite typography, `cx()`, the base layer's React Aria contract), which
used to be duplicated here as an auto-installed rules block and drifted.

This file used to carry its own copy of the project description too. The copy
drifted — it was still calling the UI layer "shadcn/ui planned" well after the
move to HeroUI v3, and it had never picked up the sections on the message tree,
the transcript scroller, logging or Android. A second description earns nothing
and goes stale in silence, so this one points at the first instead.
